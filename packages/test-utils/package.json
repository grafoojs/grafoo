{
  "private": true,
  "name": "@grafoo/test-utils",
  "version": "0.0.1",
  "main": "dist/index.js",
  "scripts": {
    "prepare": "yarn build # babel-plugin-tag",
    "build": "babel src --out-dir dist && cpy src/schema.graphql dist"
  }
}
